[pytest]
pythonpath = .
addopts = -v
markers =
    smoke: quick critical-path checks (login, can send a question)
    regression: broader checks on existing behavior
    performance: response-time / speed checks